# Local (M4 / MPS) environment — Part B: head training + inference + eval.
# Python 3.11, venv `mammalps-env`. After a successful install, freeze the exact
# resolved versions with:  mammalps-env/bin/pip freeze > requirements-local.lock.txt

# --- Deep learning (macOS arm64 wheels ship MPS; no CUDA on mac) ---
torch>=2.2
torchaudio>=2.2
transformers>=4.40

# --- Numerics / data ---
numpy>=1.24,<3
pandas>=2.0
pyarrow>=14
scikit-learn>=1.3

# --- Audio / video IO (PyAV preferred over decord on arm64 macOS) ---
librosa>=0.10
soundfile>=0.12
av>=11
opencv-python>=4.8

# --- Config / utils ---
pyyaml>=6
h5py>=3.10
tqdm>=4.66
remotezip>=0.12   # pull small files (split CSVs) from the 88GB remote zip via HTTP range

# --- Official MammAlps repo deps (data + eval helpers) ---
supervision>=0.1.0
ffmpeg-python
ipywidgets

# --- Dev ---
pytest>=8
ruff>=0.15   # formatter + linter (format-compatible with black)
