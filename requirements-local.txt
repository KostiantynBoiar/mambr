# ZoomTrack — local env (Python 3.11, venv zoomtrack-env). Part B iterates here (select /
# fuse / track / eval) plus the mock backbone; no GPU or SAM weights needed. The heavy
# backbone + veval deps live in requirements-gpu.txt (the Part-A caching box).
# After a clean install:  zoomtrack-env/bin/pip freeze > requirements-local.lock.txt

numpy>=1.24,<3
torch>=2.2            # MPS on macOS arm64; CUDA on the caching box
pycocotools>=2.0.7   # RLE decode/encode for SA-FARI masklets + VEval JSON
opencv-python>=4.8   # connected-components (mock backbone), resize, crop/paste
pyyaml>=6            # YAML config loading
tqdm>=4.66           # progress on the caching pass

# --- dev ---
pytest>=8
ruff>=0.15
