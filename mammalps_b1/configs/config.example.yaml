# Copy to config.local.yaml (gitignored) and override only what you need — every key
# has a default in mammalps_b1/config.py. Pass with: --config mammalps_b1/configs/<file>.yaml
# Paths may be omitted to fall back to $MAMMALPS_DATA_ROOT / $MAMMALPS_CACHE_ROOT.

paths:
  data_root: ~/Documents/Programming/University/MSc_dissertation/data/mammalps_v1
  cache_root: ~/Documents/Programming/University/MSc_dissertation/cache

modalities: [video]        # [video] or [video, audio] (later: + mask)
seed: 0                    # alias for train.seed

# --- everything below is optional; shown with its default value ---
# model:
#   fusion_dim: 256
#   hidden_dim: 256
#   dropout: 0.1
# train:
#   loss_weights: {Spe: 1.0, ActY: 2.5, ActN: 2.0}
#   num_test_segments: 10
