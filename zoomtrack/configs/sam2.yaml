# SAM 2 fallback backbone (box/point prompt). Set weights before running.
seed: 0

backbone:
  name: sam2
  weights: ""          # path/id of the frozen SAM 2 checkpoint

selector:
  mode: heuristic
  tile_budget: 8
