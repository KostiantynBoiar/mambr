# SAM 3 primary backbone (phrase prompt). Set weights before running.
seed: 0

backbone:
  name: sam3
  weights: ""          # path/id of the frozen SAM 3 checkpoint

selector:
  mode: heuristic
  tile_budget: 8
