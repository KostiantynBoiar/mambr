# Mock backbone — tests / CI / laptop (no weights, no GPU).
seed: 0

backbone:
  name: mock
  min_area: 256        # blobs below this in the downscaled frame are dropped (the small-animal miss)

selector:
  mode: heuristic
  tile_budget: 8
