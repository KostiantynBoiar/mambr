"""MABe22 contact-geometry — PRIMARY track (mask-contact geometry vs keypoint hulls).

Scaffold only; logic is implemented one stage at a time (see ../ROADMAP.md, M0–M9).
Reuses the frozen-encoder / config / fusion-head patterns from the MammAlps track (or
``common/`` once shared code is extracted).
"""
